// https://ample-tortoise-57.clerk.accounts.dev

const authConfig = {
    providers: [
      {
        domain: "https://literate-fawn-15.clerk.accounts.dev",
        applicationID: "convex",
      },
    ]
  };
  
  export default authConfig;